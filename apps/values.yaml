project: bl01t
destination:
  name: pollux
  namespace: bl01t
source:
  repoURL: https://github.com/gilesknap/bl02t.git
  targetRevision: main

# NOTE: fields that appear here will show up in the root app parameters and can
# therefore be overriden in the UI (or CLI).
# Fields added in 'values:' in the all_apps.yaml template are not overrideable
# AFAIK (but fields added in the  parameters section are)
services:
  epics-pvcs:
  epics-opis:
    enabled: false
  bl02t-ea-test-01:
    enabled: true
    files: ["ioc.yaml", "ioc.db"]
